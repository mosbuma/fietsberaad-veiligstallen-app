import * as React from "react";
// import Input from '@mui/material/TextField';

function PageTitle({
  children
}) {
  return (
    <h1 className="
    ">
      {children}
    </h1>
  );
}

export default PageTitle;
